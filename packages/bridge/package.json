{
  "name": "pi-plan-bridge",
  "version": "1.0.0",
  "type": "module",
  "description": "Shared contract for pi-plan-* extensions: plan/build + safety types, capability interfaces (SafetyProvider / PlanPreviewRenderer), event channel constants, and pi.events RPC helpers.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-plan"
  ],
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/bridge.spec.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
